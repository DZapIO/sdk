/node_modules
/build
.DS_Store
yarn-error.log
.env
.eslintcache
/.idea
dist/
electron-builder.yml
/.yalc
yalc.lock
yarn.lock
# testing
/coverage/
src/types/contracts/
src/types/gateway/
tsconfig.tsbuildinfo
public/**/*.js
jest.results.json
*.#*
bun.lockb

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# headroom's per-session wrapper state (live pid/port + env-var ownership).
.claude/.headroom_wrap_*
